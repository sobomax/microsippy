if [ -z "${IDF_TOOLCHAIN}" -o -z "${IDF_PATH}" ]
then
  echo "IDF_TOOLCHAIN and IDF_PATH need to be set" >&2
  exit 1
fi
if [ -z "${TOOLSDIR}" ]
then
  echo "TOOLSDIR needs to be set" >&2
  exit 1
fi

SRCDIR="${TOOLSDIR}/../platforms/freertos/esp8266"
BUILDDIR="${SRCDIR}/build"
DIFF="diff -u"
